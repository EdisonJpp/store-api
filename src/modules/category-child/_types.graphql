type CategoryChild {
  id: Int!
  name: String!
  slug: String
  params: [CategoryChildParam]
}

type Query {
  categoriesChildByParent(categoryParentId: Int!): [CategoryChild]
  categoryChild(categoryChildId: Int!): CategoryChild
}
